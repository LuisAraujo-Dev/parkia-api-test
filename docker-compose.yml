version: '3.8'
services:
  db:
    image: postgres:15
    container_name: parkia_db
    restart: always
    environment:
      POSTGRES_USER: parkia_user
      POSTGRES_PASSWORD: parkia_password
      POSTGRES_DB: parkia_db
    ports:
      - "5432:5432" 